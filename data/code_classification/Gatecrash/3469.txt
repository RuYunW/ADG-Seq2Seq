public class SignalTheClans extends CardImpl {°Ïpublic SignalTheClans (UUID ownerId) {°Ïsuper(ownerId, 194, "Signal the Clans", Rarity.RARE, new CardType[]{CardType.INSTANT}, "{R}{G}");°Ïthis.expansionSetCode = "GTC";°Ïthis.getSpellAbility().addEffect(new SignalTheClansEffect());°Ï}°Ïpublic SignalTheClans(final SignalTheClans card) {°Ïsuper(card);°Ï}°Ï@Override°Ïpublic SignalTheClans copy() {°Ïreturn new SignalTheClans(this);°Ï}°Ï}°Ïclass SignalTheClansEffect extends SearchEffect {°Ïpublic SignalTheClansEffect() {°Ïsuper(new TargetCardInLibrary(3, new FilterCreatureCard()), Outcome.DrawCard);°ÏstaticText = "Search your library for three creature cards and reveal them. If you reveal three cards with different names, choose one of them at random and put that card into your hand. Shuffle the rest into your library";°Ï}°Ïpublic SignalTheClansEffect(final SignalTheClansEffect effect) {°Ïsuper(effect);°Ï}°Ï@Override°Ïpublic SignalTheClansEffect copy() {°Ïreturn new SignalTheClansEffect(this);°Ï}°Ï@Override°Ïpublic boolean apply(Game game, Ability source) {°ÏPlayer player = game.getPlayer(source.getControllerId());°Ïif (player == null) {°Ïreturn false;°Ï}°Ïif (player.searchLibrary(target, game)) {°Ïif (target.getTargets().size() > 0) {°ÏCards cards = new CardsImpl();°Ïfor (UUID cardId: (List<UUID>)target.getTargets()) {°ÏCard card = player.getLibrary().remove(cardId, game);°Ïif (card != null){°Ïcards.add(card);°Ï}°Ï}°Ïplayer.revealCards("Reveal", cards, game);°ÏCard cardsArray[] = cards.getCards(game).toArray(new Card[0]);°Ïif(cardsArray.length == 3 && !cardsArray[0].getName().equals(cardsArray[1]) && !cardsArray[0].getName().equals(cardsArray[2]) && !cardsArray[1].getName().equals(cardsArray[2])){°ÏCard randomCard = cards.getRandom(game);°ÏrandomCard.moveToZone(Zone.HAND, source.getSourceId(), game, true);°Ïcards.remove(randomCard);°Ï}°Ïfor(Card card : cards.getCards(game)){°Ïcard.moveToZone(Zone.LIBRARY, source.getSourceId(), game, true);°Ï}°Ï}°Ïplayer.shuffleLibrary(game);°Ïreturn true;°Ï}°Ïplayer.shuffleLibrary(game);°Ïreturn false;°Ï}°Ï}°Ï
