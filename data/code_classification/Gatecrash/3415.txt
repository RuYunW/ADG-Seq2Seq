public class LuminatePrimordial extends CardImpl {°Ïpublic LuminatePrimordial(UUID ownerId) {°Ïsuper(ownerId, 20, "Luminate Primordial", Rarity.RARE, new CardType[]{CardType.CREATURE}, "{5}{W}{W}");°Ïthis.expansionSetCode = "GTC";°Ïthis.subtype.add("Avatar");°Ïthis.power = new MageInt(4);°Ïthis.toughness = new MageInt(7);°Ïthis.addAbility(VigilanceAbility.getInstance());°Ïthis.addAbility(new EntersBattlefieldTriggeredAbility(new LuminatePrimordialEffect(),false));°Ï}°Ï@Override°Ïpublic void adjustTargets(Ability ability, Game game) {°Ïif (ability instanceof EntersBattlefieldTriggeredAbility) {°Ïability.getTargets().clear();°Ïfor(UUID opponentId : game.getOpponents(ability.getControllerId())) {°ÏPlayer opponent = game.getPlayer(opponentId);°Ïif (opponent != null) {°ÏFilterCreaturePermanent filter = new FilterCreaturePermanent("creature from opponent " + opponent.getLogName());°Ïfilter.add(new ControllerIdPredicate(opponentId));°ÏTargetCreaturePermanent target = new TargetCreaturePermanent(0,1, filter,false);°Ïability.addTarget(target);°Ï}°Ï}°Ï}°Ï}°Ïpublic LuminatePrimordial(final LuminatePrimordial card) {°Ïsuper(card);°Ï}°Ï@Override°Ïpublic LuminatePrimordial copy() {°Ïreturn new LuminatePrimordial(this);°Ï}°Ï}°Ïclass LuminatePrimordialEffect extends OneShotEffect {°Ïpublic LuminatePrimordialEffect() {°Ïsuper(Outcome.Benefit);°Ïthis.staticText = "for each opponent, exile up to one target creature that player controls and that player gains life equal to its power";°Ï}°Ïpublic LuminatePrimordialEffect(final LuminatePrimordialEffect effect) {°Ïsuper(effect);°Ï}°Ï@Override°Ïpublic LuminatePrimordialEffect copy() {°Ïreturn new LuminatePrimordialEffect(this);°Ï}°Ï@Override°Ïpublic boolean apply(Game game, Ability source) {°Ïfor (Target target: source.getTargets()) {°Ïif (target instanceof TargetCreaturePermanent) {°ÏPermanent targetCreature = game.getPermanent(target.getFirstTarget());°Ïif (targetCreature != null && !targetCreature.getControllerId().equals(source.getControllerId())) {°Ïint amountLife = targetCreature.getPower().getValue();°ÏPlayer controller = game.getPlayer(targetCreature.getControllerId());°ÏtargetCreature.moveToExile(null, null, source.getSourceId(), game);°Ïif (controller != null && amountLife != 0) {°Ïcontroller.gainLife(amountLife, game);°Ï}°Ï}°Ï}°Ï}°Ïreturn true;°Ï}°Ï}°Ï
