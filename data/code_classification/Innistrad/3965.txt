public class ParallelLives extends CardImpl {°Ïpublic ParallelLives(UUID ownerId) {°Ïsuper(ownerId, 199, "Parallel Lives", Rarity.RARE, new CardType[]{CardType.ENCHANTMENT}, "{3}{G}");°Ïthis.expansionSetCode = "ISD";°Ïthis.addAbility(new SimpleStaticAbility(Zone.BATTLEFIELD, new ParallelLivesEffect()));°Ï}°Ïpublic ParallelLives(final ParallelLives card) {°Ïsuper(card);°Ï}°Ï@Override°Ïpublic ParallelLives copy() {°Ïreturn new ParallelLives(this);°Ï}°Ï}°Ïclass ParallelLivesEffect extends ReplacementEffectImpl {°Ïpublic ParallelLivesEffect() {°Ïsuper(Duration.WhileOnBattlefield, Outcome.Copy);°ÏstaticText = "If an effect would put one or more tokens onto the battlefield under your control, it puts twice that many of those tokens onto the battlefield instead";°Ï}°Ïpublic ParallelLivesEffect(final ParallelLivesEffect effect) {°Ïsuper(effect);°Ï}°Ï@Override°Ïpublic ParallelLivesEffect copy() {°Ïreturn new ParallelLivesEffect(this);°Ï}°Ï@Override°Ïpublic boolean checksEventType(GameEvent event, Game game) {°Ïreturn event.getType().equals(EventType.CREATE_TOKEN);°Ï}°Ï@Override°Ïpublic boolean applies(GameEvent event, Ability source, Game game) {°ÏStackObject spell = game.getStack().getStackObject(event.getSourceId());°Ïreturn spell != null && spell.getControllerId().equals(source.getControllerId());°Ï}°Ï@Override°Ïpublic boolean apply(Game game, Ability source) {°Ïreturn true;°Ï}°Ï@Override°Ïpublic boolean replaceEvent(GameEvent event, Ability source, Game game) {°Ïevent.setAmount(event.getAmount() * 2);°Ïreturn false;°Ï}°Ï}°Ï
