public class Mulch extends CardImpl {°Ïpublic Mulch(UUID ownerId) {°Ïsuper(ownerId, 196, "Mulch", Rarity.COMMON, new CardType[]{CardType.SORCERY}, "{1}{G}");°Ïthis.expansionSetCode = "ISD";°Ïthis.getSpellAbility().addEffect(new MulchEffect());°Ï}°Ïpublic Mulch(final Mulch card) {°Ïsuper(card);°Ï}°Ï@Override°Ïpublic Mulch copy() {°Ïreturn new Mulch(this);°Ï}°Ï}°Ïclass MulchEffect extends OneShotEffect {°Ïpublic MulchEffect() {°Ïsuper(Outcome.DrawCard);°Ïthis.staticText = "Reveal the top four cards of your library. Put all land cards revealed this way into your hand and the rest into your graveyard";°Ï}°Ïpublic MulchEffect(final MulchEffect effect) {°Ïsuper(effect);°Ï}°Ï@Override°Ïpublic MulchEffect copy() {°Ïreturn new MulchEffect(this);°Ï}°Ï@Override°Ïpublic boolean apply(Game game, Ability source) {°ÏPlayer controller = game.getPlayer(source.getControllerId());°ÏMageObject sourceObject = game.getObject(source.getSourceId());°Ïif (controller != null && sourceObject != null) {°ÏCards cards = new CardsImpl();°Ïcards.addAll(controller.getLibrary().getTopCards(game, 4));°Ïif (!cards.isEmpty()) {°Ïcontroller.revealCards(sourceObject.getName(), cards, game);°ÏCards landCards = new CardsImpl();°ÏCards otherCards = new CardsImpl();°Ïfor (Card card: cards.getCards(game)) {°Ïif (card.getCardType().contains(CardType.LAND)) {°ÏlandCards.add(card);°Ï} else {°ÏotherCards.add(card);°Ï}°Ï}°Ïcontroller.moveCards(landCards, Zone.LIBRARY, Zone.HAND, source, game);°Ïcontroller.moveCards(otherCards, Zone.LIBRARY, Zone.GRAVEYARD, source, game);°Ï}°Ïreturn true;°Ï}°Ïreturn false;°Ï}°Ï}°Ï
