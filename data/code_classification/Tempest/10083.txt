public class Intuition extends CardImpl {°Ïpublic Intuition(UUID ownerId) {°Ïsuper(ownerId, 70, "Intuition", Rarity.RARE, new CardType[]{CardType.INSTANT}, "{2}{U}");°Ïthis.expansionSetCode = "TMP";°Ïthis.getSpellAbility().addEffect(new IntuitionEffect());°Ïthis.getSpellAbility().addTarget(new TargetOpponent());°Ï}°Ïpublic Intuition(final Intuition card) {°Ïsuper(card);°Ï}°Ï@Override°Ïpublic Intuition copy() {°Ïreturn new Intuition(this);°Ï}°Ï}°Ïclass IntuitionEffect extends SearchEffect {°Ïpublic IntuitionEffect() {°Ïsuper(new TargetCardInLibrary(3, new FilterCard()), Outcome.Benefit);°ÏstaticText = "Search your library for three cards and reveal them. Target opponent chooses one. Put that card into your hand and the rest into your graveyard. Then shuffle your library";°Ï}°Ïpublic IntuitionEffect(final IntuitionEffect effect) {°Ïsuper(effect);°Ï}°Ï@Override°Ïpublic IntuitionEffect copy() {°Ïreturn new IntuitionEffect(this);°Ï}°Ï@Override°Ïpublic boolean apply(Game game, Ability source) {°ÏPlayer controller = game.getPlayer(source.getControllerId());°ÏPlayer opponent = game.getPlayer(source.getFirstTarget());°Ïif (controller == null || opponent == null)°Ïreturn false;°Ïif (controller.getLibrary().size() >= 3 && controller.searchLibrary(target, game)) {°Ïif (target.getTargets().size() == 3) {°ÏCards cards = new CardsImpl();°Ïfor (UUID cardId: (List<UUID>)target.getTargets()) {°ÏCard card = controller.getLibrary().getCard(cardId, game);°Ïif (card != null) {°Ïcards.add(card);°Ï}°Ï}°Ïcontroller.revealCards("Reveal", cards, game);°ÏTargetCard targetCard = new TargetCard(Zone.LIBRARY, new FilterCard());°Ïwhile(!opponent.choose(Outcome.Neutral, cards, targetCard, game)) {°Ïif (!opponent.canRespond()) {°Ïreturn false;°Ï}°Ï}°ÏCard card = cards.get(targetCard.getFirstTarget(), game);°Ïif (card != null) {°Ïcards.remove(card);°Ïcontroller.moveCards(card, Zone.LIBRARY, Zone.HAND, source, game);°Ï}°Ïcontroller.moveCards(cards, Zone.LIBRARY, Zone.GRAVEYARD, source, game);°Ï}°Ïcontroller.shuffleLibrary(game);°Ïreturn true;°Ï}°Ïcontroller.shuffleLibrary(game);°Ïreturn false;°Ï}°Ïpublic List<UUID> getTargets() {°Ïreturn target.getTargets();°Ï}°Ï}°Ï
